import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.lovable.efootballcompetitionbetting',
  appName: 'ECB',
  webDir: 'dist',
  bundledWebRuntime: false,
};

export default config;
