{
  "whatsNew": [
    "Fixed mobile WebView scaling so the site uses the phone screen correctly instead of appearing zoomed out.",
    "Update downloads now happen inside the app and launch the Android installer directly instead of opening the browser.",
    "Improved update download progress and failure handling.",
    "App-local account and WebView storage remain separate from Chrome browser data."
  ]
}
